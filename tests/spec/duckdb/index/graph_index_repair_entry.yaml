# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_repair_entry
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: After deleting the row that backs the HNSW entry node, search must
steps:
- statement: SET ${GUC_BF_THRESHOLD} = 0;
- statement: CREATE TABLE entry_test (id INTEGER, vec ${VECTOR(4)});
- statement: INSERT INTO entry_test SELECT i, ${VEC_LITERAL([(i*0.1)::FLOAT, (i*0.2)::FLOAT, (i*0.3)::FLOAT, (i*0.4)::FLOAT], 4)} FROM ${RANGE(200)} t(i);
- statement: CREATE INDEX idx_entry ON entry_test USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- query: SELECT id FROM entry_test ORDER BY ${L2(vec, ${VEC_LITERAL([10.0, 20.0, 30.0, 40.0], 4)})} LIMIT 1;
  _field_types: I
  expect:
  - - 100
- statement: DELETE FROM entry_test WHERE id < 50;
- query: SELECT COUNT(*) FROM entry_test;
  _field_types: I
  expect:
  - - 150
- query: SELECT id FROM entry_test ORDER BY ${L2(vec, ${VEC_LITERAL([10.0, 20.0, 30.0, 40.0], 4)})} LIMIT 1;
  _field_types: I
  expect:
  - - 100
- query: "SELECT COUNT(*) FROM (\n    SELECT id FROM entry_test\n    ORDER BY ${L2(vec, ${VEC_LITERAL([10.0, 20.0, 30.0, 40.0], 4)})}\n    LIMIT 3\n) WHERE id BETWEEN 99 AND 101;"
  _field_types: I
  expect:
  - - 3
- statement: DROP TABLE entry_test;
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_repair_entry.test

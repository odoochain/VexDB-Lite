# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_params
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test graph index creation with custom parameters
steps:
- statement: CREATE TABLE vecs (id INTEGER, vec ${VECTOR(4)});
- statement: INSERT INTO vecs SELECT i, ${VEC_LITERAL([(i * 0.1)::FLOAT, (i * 0.2)::FLOAT, (i * 0.3)::FLOAT, (i * 0.4)::FLOAT], 4)} FROM ${RANGE(200)} t(i);
- statement: CREATE INDEX idx_default ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- statement: DROP INDEX idx_default;
- statement: CREATE INDEX idx_m8 ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_WITH_L2(m = 8)};
- statement: DROP INDEX idx_m8;
- statement: CREATE INDEX idx_ef ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_WITH_L2(ef_construction = 128)};
- statement: DROP INDEX idx_ef;
- statement: CREATE INDEX idx_both ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_WITH_L2(m = 32, ef_construction = 200)};
- query: SELECT id FROM vecs ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0, 0.0], 4)})} LIMIT 1;
  _field_types: I
  expect:
  - - 0
- statement: CREATE INDEX idx_bad ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_WITH_L2(m = 1)};
  expect_error: ''
- statement: CREATE INDEX idx_bad2 ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_WITH_L2(m = 200)};
  expect_error: ''
- statement: DROP TABLE vecs;
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_params.test

# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_distance
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test distance query with graph index
steps:
- statement: CREATE TABLE items (id INTEGER, vec ${VECTOR(3)});
- statement: INSERT INTO items VALUES (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)});
- statement: INSERT INTO items VALUES (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)});
- statement: INSERT INTO items VALUES (3, ${VEC_LITERAL([0.0, 0.0, 1.0], 3)});
- query: SELECT id, vec, ${L2(vec, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)})} as dist FROM items ORDER BY dist;
  _field_types: III
  expect:
  - - 1
    - '[1.0, 0.0, 0.0]'
    - 0
  - - 2
    - '[0.0, 1.0, 0.0]'
    - 1.4142135623730951
  - - 3
    - '[0.0, 0.0, 1.0]'
    - 1.4142135623730951
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_distance.test

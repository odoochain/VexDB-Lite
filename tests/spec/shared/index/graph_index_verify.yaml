# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_verify
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Verify vex extension and index registration
steps:
- statement: CREATE TABLE test (vec ${VECTOR(3)});
- statement: INSERT INTO test VALUES (${VEC_LITERAL([1.0, 2.0, 3.0], 3)});
- query: SELECT vector_dims(vec) FROM test;
  _field_types: I
  expect:
  - - 3
- query: SELECT ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} FROM test;
  _field_types: R
  expect:
  - - 3.7416573867739413
- statement: CREATE INDEX test_idx ON test USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- query: SELECT COUNT(*) FROM ${SYS_INDEXES} WHERE ${SYS_INDEXES_NAME} = 'test_idx';
  _field_types: I
  expect:
  - - 1
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_verify.test

skip:
  sqlite: true

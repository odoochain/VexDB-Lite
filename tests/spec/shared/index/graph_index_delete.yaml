# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_delete
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test graph index delete and vacuum operations
steps:
- statement: CREATE TABLE vecs (id INTEGER, vec ${VECTOR(3)});
- statement: "INSERT INTO vecs VALUES\n  (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}),\n  (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)}),\n  (3, ${VEC_LITERAL([0.0, 0.0, 1.0], 3)}),\n  (4, ${VEC_LITERAL([1.0, 1.0,\
    \ 0.0], 3)}),\n  (5, ${VEC_LITERAL([0.0, 1.0, 1.0], 3)}),\n  (6, ${VEC_LITERAL([1.0, 0.0, 1.0], 3)}),\n  (7, ${VEC_LITERAL([0.5, 0.5, 0.5], 3)}),\n  (8, ${VEC_LITERAL([0.2, 0.2, 0.2], 3)});"
- statement: CREATE INDEX idx ON vecs USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- query: SELECT id FROM vecs ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 8
- statement: DELETE FROM vecs WHERE id = 8;
- query: SELECT id FROM vecs ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 7
- statement: DELETE FROM vecs WHERE id IN (1, 2, 3);
- query: SELECT count(*) FROM vecs;
  _field_types: I
  expect:
  - - 4
- statement: INSERT INTO vecs VALUES (9, ${VEC_LITERAL([0.1, 0.1, 0.1], 3)});
- query: SELECT id FROM vecs ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 9
- statement: DROP TABLE vecs;
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_delete.test

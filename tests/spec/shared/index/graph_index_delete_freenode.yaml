# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_delete_freenode
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Verify synchronous FreeNode in Delete - node_count correctness and allocator segment reclamation
steps:
- statement: CREATE TABLE t1 (id INTEGER, vec ${VECTOR(3)});
- statement: "INSERT INTO t1 VALUES\n  (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}),\n  (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)});"
- statement: CREATE INDEX idx1 ON t1 USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- statement: DELETE FROM t1 WHERE id = 1;
- query: SELECT count(*) FROM (SELECT id FROM t1 ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)})} LIMIT 1);
  _field_types: I
  expect:
  - - 1
- query: SELECT id FROM t1 ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 2
- statement: DROP TABLE t1;
- statement: CREATE TABLE t2 (id INTEGER, vec ${VECTOR(3)});
- statement: "INSERT INTO t2 VALUES\n  (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}),\n  (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)}),\n  (3, ${VEC_LITERAL([0.0, 0.0, 1.0], 3)}),\n  (4, ${VEC_LITERAL([0.5, 0.5, 0.0],\
    \ 3)}),\n  (5, ${VEC_LITERAL([0.0, 0.5, 0.5], 3)});"
- statement: CREATE INDEX idx2 ON t2 USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- statement: DELETE FROM t2 WHERE id IN (1, 2, 3, 4);
- query: SELECT id FROM t2 ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 5
- statement: INSERT INTO t2 VALUES (6, ${VEC_LITERAL([0.1, 0.1, 0.1], 3)});
- query: SELECT count(*) FROM (SELECT id FROM t2 ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 2);
  _field_types: I
  expect:
  - - 2
- statement: DROP TABLE t2;
- statement: CREATE TABLE t3 (id INTEGER, vec ${VECTOR(3)});
- statement: "INSERT INTO t3 VALUES\n  (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}),\n  (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)});"
- statement: CREATE INDEX idx3 ON t3 USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- statement: DELETE FROM t3;
- query: SELECT count(*) FROM (SELECT id FROM t3 ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0], 3)})} LIMIT 5);
  _field_types: I
  expect:
  - - 0
- statement: INSERT INTO t3 VALUES (3, ${VEC_LITERAL([0.5, 0.5, 0.5], 3)});
- query: SELECT id FROM t3 ORDER BY ${L2(vec, ${VEC_LITERAL([0.5, 0.5, 0.5], 3)})} LIMIT 1;
  _field_types: I
  expect:
  - - 3
- statement: DROP TABLE t3;
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_delete_freenode.test

skip:
  sqlite: true

# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_simple
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Simple test for graph index
steps:
- statement: CREATE TABLE test_vectors (id INTEGER, vec ${VECTOR(3)});
- statement: INSERT INTO test_vectors VALUES (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)});
- statement: CREATE INDEX test_idx ON test_vectors USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- query: SELECT COUNT(*) FROM ${SYS_INDEXES} WHERE ${SYS_INDEXES_NAME} = 'test_idx';
  _field_types: I
  expect:
  - - 1
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_simple.test

skip:
  sqlite: true

# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_search
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test graph index vector search (ANN)
steps:
- statement: CREATE TABLE items (id INTEGER, vec ${VECTOR(3)});
- statement: "INSERT INTO items VALUES\n  (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}),\n  (2, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)}),\n  (3, ${VEC_LITERAL([0.0, 0.0, 1.0], 3)}),\n  (4, ${VEC_LITERAL([0.9, 0.1,\
    \ 0.0], 3)}),\n  (5, ${VEC_LITERAL([0.0, 0.9, 0.1], 3)}),\n  (6, ${VEC_LITERAL([0.1, 0.0, 0.9], 3)});"
- statement: CREATE INDEX idx_items ON items USING ${VEX_INDEX} (vec${OPS_L2_COL})${IDX_OPTS_L2};
- query: SELECT id FROM items ORDER BY ${L2(vec, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)})} LIMIT 3;
  _field_types: I
  expect:
  - - 1
  - - 4
  - - 6
- query: SELECT id FROM items ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)})} LIMIT 3;
  _field_types: I
  expect:
  - - 2
  - - 5
  - - 4
_migrated_from: vexdb_duckdb/test/sql/vex/index/graph_index_search.test

skip:
  sqlite: true

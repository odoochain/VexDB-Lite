# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: types__float_array_basic
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test FLOAT[N] vector type basics
steps:
- statement: CREATE TABLE t1 (id INT, embedding ${VECTOR(3)});
- statement: INSERT INTO t1 VALUES (1, ${VEC_LITERAL([1.0, 2.0, 3.0], 3)});
- statement: INSERT INTO t1 VALUES (2, ${VEC_LITERAL([4.0, 5.0, 6.0], 3)});
- query: SELECT id, embedding FROM t1 ORDER BY id;
  _field_types: II
  expect:
  - - 1
    - '[1.0, 2.0, 3.0]'
  - - 2
    - '[4.0, 5.0, 6.0]'
- statement: INSERT INTO t1 VALUES (3, ${VEC_LITERAL([1.0, 2.0], 2)});
  expect_error: ''
- statement: CREATE TABLE t2 (v ${VECTOR(1)});
- statement: INSERT INTO t2 VALUES (${VEC_LITERAL([42.0], 1)});
- query: SELECT v FROM t2;
  _field_types: I
  expect:
  - - '[42.0]'
- statement: INSERT INTO t1 VALUES (4, NULL);
- query: SELECT id, embedding FROM t1 WHERE embedding IS NULL;
  _field_types: II
  expect:
  - - 4
    - 'NULL'
_migrated_from: vexdb_duckdb/test/sql/vex/types/float_array_basic.test

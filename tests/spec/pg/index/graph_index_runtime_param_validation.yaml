# auto-migrated from pg_tests/ — PG 专属用例, 不模板化
name: graph_index_runtime_param_validation
tags:
- pg-only
- migrated
description: Migrated from pg_tests/index_basic/graph_index_runtime_param_validation.sql
engines:
- pg
- opengauss
steps:
- statement: CREATE TABLE t_param (id INTEGER, vec floatvector(4));
- statement: INSERT INTO t_param SELECT i, ('['||((i*0.1)::FLOAT)::text||','||((i*0.2)::FLOAT)::text||','||((i*0.3)::FLOAT)::text||','||((i*0.4)::FLOAT)::text||']')::floatvector FROM generate_series(0,
    99) t(i);
- statement: CREATE INDEX idx_param ON t_param USING vexdb_graph (vec);
- statement: SET vexdb_vector.ef_search = -1;
- query: SELECT * FROM t_param ORDER BY l2_distance(vec, '[0.1, 0.2, 0.3, 0.4]'::floatvector) LIMIT 5;
  expect:
  - - ERROR ~ vexdb_vector.ef_search
- statement: SET vexdb_vector.ef_search = 0;
- query: SELECT * FROM t_param ORDER BY l2_distance(vec, '[0.1, 0.2, 0.3, 0.4]'::floatvector) LIMIT 5;
  expect:
  - - ERROR ~ vexdb_vector.ef_search
- statement: SET vexdb_vector.ef_search = 99999;
- query: SELECT * FROM t_param ORDER BY l2_distance(vec, '[0.1, 0.2, 0.3, 0.4]'::floatvector) LIMIT 5;
  expect:
  - - ERROR ~ vexdb_vector.ef_search
- statement: SET vexdb_vector.ef_search = 40;
- query: SELECT count(*) FROM (SELECT * FROM t_param ORDER BY l2_distance(vec, '[0.1, 0.2, 0.3, 0.4]'::floatvector) LIMIT 5);
  expect:
  - - 5
- statement: SET vexdb_vector.ef_search = 5000;
- query: SELECT count(*) FROM (SELECT * FROM t_param ORDER BY l2_distance(vec, '[0.1, 0.2, 0.3, 0.4]'::floatvector) LIMIT 5);
  expect:
  - - 5
- statement: RESET vexdb_vector.ef_search;
- statement: DROP TABLE t_param;
